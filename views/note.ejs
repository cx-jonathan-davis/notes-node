<%- include('partials/header', { title: note.title, query: query }) %>

  <article class="note">
    <h1><%= note.title %></h1>
    <time><%= note.created_at %></time>
    <p><%= note.body %></p>
  </article>
  <p><a href="/">&larr; Back to all notes</a></p>

<%- include('partials/footer') %>
